- name: Dotfiles
  hosts: localhost
  connection: local
  pre_tasks:
    - name: Detect 1Password
      ansible.builtin.import_tasks: pre_tasks/detect_1password.yml
  roles:
    - system
    - dotfiles
